#define __CONFIG_H
#define MAXMEM 16777216		// bytes of memory mapped 
/*
A static memory configuration is used as the amount of memory an embedded system has is unlikely to change over time.
For hobbyists, it is trivial to change this number in the source code, so this simple solution is good enough. 
*/